import { GlassPanel, StatusBadge } from "@/components/domain/ui";

export type EntityRow = Record<string, string | number | null>;
const valueLabels: Record<string, string> = { DRAFT: "Borrador", SENT: "Enviada", ACCEPTED: "Aceptada", CONFIRMED: "Confirmada", PENDING: "Pendiente", CANCELLED: "Cancelada", OPEN: "Abierta", PAID: "Pagada", OVERDUE: "Vencida", ACTIVE: "Activo", INACTIVE: "Inactivo", TODO: "Pendiente", IN_PROGRESS: "En curso", HIGH: "Alta", MEDIUM: "Media", LOW: "Baja" };
export function EntityTable({ title, description, rows }: { title: string; description: string; rows: EntityRow[] }) { const columns = rows.length ? Object.keys(rows[0]) : []; return <GlassPanel className="overflow-hidden"><div className="flex items-center justify-between p-5"><div><h2 className="text-sm font-semibold">{title}</h2><p className="mt-1 text-xs text-muted">{description}</p></div><span className="text-xs text-muted">{rows.length} registros</span></div><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="border-y bg-ink/[.025] text-[10px] uppercase tracking-[.1em] text-muted"><tr>{columns.map((column) => <th key={column} className="px-5 py-2.5 font-medium">{column.replaceAll("_", " ")}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr key={index} className="border-b border-line/50 last:border-0 hover:bg-ink/[.025]">{columns.map((column) => <td key={column} className="whitespace-nowrap px-5 py-3">{column === "status" && typeof row[column] === "string" && ["confirmed","requested","pending","cancelled"].includes(row[column].toLowerCase()) ? <StatusBadge status={row[column].toLowerCase() as "confirmed" | "requested" | "pending" | "cancelled"}/> : (typeof row[column] === "string" ? (valueLabels[row[column]] ?? row[column]) : (row[column] ?? "—"))}</td>)}</tr>)}</tbody></table></div></GlassPanel>; }
