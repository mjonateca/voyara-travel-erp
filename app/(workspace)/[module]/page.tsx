import { AppShell } from "@/components/app-shell";
import { EmptyState, PageHeader } from "@/components/domain/ui";

const info: Record<string, [string, string, string]> = { sales: ["Sales workspace", "Quotes, bookings and commercial performance will be available here.", "Sales"], crm: ["Customers & agencies", "A unified view of customers, agencies and their commercial history.", "CRM"], products: ["Product catalogue", "Product and contracting capabilities are planned for Phase 2.", "Product"], operations: ["Operations control", "Daily departures and resource coordination will be activated in Phase 8.", "Operations"], finance: ["Finance workspace", "Invoices, payments and reconciliation will be activated in Phase 9.", "Finance"], administration: ["Administration", "Tenant, company, office, users and role management foundation.", "Platform"], settings: ["Settings", "Tenant configuration and integrations are managed here.", "Platform"] };
export default async function ModulePage({ params }: { params: Promise<{ module: string }> }) { const { module } = await params; const [title, description, eyebrow] = info[module] ?? ["Workspace", "This workspace is being configured.", "Voyara"]; return <AppShell><PageHeader eyebrow={eyebrow} title={title} description={description}/><div className="mt-6"><EmptyState title="Module foundation is ready" detail="This intentionally scoped placeholder preserves navigation while the corresponding domain phase is implemented."/></div></AppShell>; }
