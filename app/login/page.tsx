import { signIn } from "./actions";

export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const { error } = await searchParams;
  return <main className="grid min-h-screen place-items-center p-5"><section className="glass w-full max-w-md rounded-2xl p-7"><div><p className="text-[11px] font-semibold uppercase tracking-[.18em] text-accent">Voyara Operations</p><h1 className="mt-2 text-2xl font-semibold">Welcome back.</h1><p className="mt-2 text-sm text-muted">Sign in to the Demo Travel Group workspace.</p></div><form action={signIn} className="mt-7 space-y-4"><label className="block text-sm font-medium">Email<input name="email" type="email" required placeholder="test1@test.com" className="mt-1.5 w-full rounded-lg border bg-surface/70 px-3 py-2.5 outline-none ring-accent/30 focus:ring-2"/></label><label className="block text-sm font-medium">Password<input name="password" type="password" required placeholder="••••" className="mt-1.5 w-full rounded-lg border bg-surface/70 px-3 py-2.5 outline-none ring-accent/30 focus:ring-2"/></label>{error && <p className="rounded-lg bg-rose-500/10 p-2.5 text-sm text-rose-700">{error}</p>}<button className="w-full rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-white">Sign in</button></form><p className="mt-5 text-xs text-muted">Demo access: test1@test.com / 1234</p></section></main>;
}
